# Program to replace odd numbers with -1

import numpy as np

array = np.array([1, 2, 3, 4, 5, 6])

# Replace odd numbers
array[array % 2 != 0] = -1

print("Modified Array:", array)
output
Modified Array: [-1  2 -1  4 -1  6]
