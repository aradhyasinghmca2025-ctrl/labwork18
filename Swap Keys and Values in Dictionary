# Program to swap keys and values

original_dict = {'a': 1, 'b': 2, 'c': 3}

# Swap using dictionary comprehension
swapped_dict = {value: key for key, value in original_dict.items()}

print("Swapped dictionary:", swapped_dict)
